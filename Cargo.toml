[package]
name = "rpx"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
[dev-dependencies]
testcontainers = { version = "0.27.2", features = ["blocking"] }
