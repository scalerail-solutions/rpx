---
title: Start a project
description: Create an installable R package project with rpx.
seo:
  title: Start an R package project with rpx
  description: Initialize an R package, select development dependencies, and create its first lockfile and project library.
sitemap:
  priority: 0.8
  changefreq: monthly
---

`rpx init` creates a new installable R package, resolves its first lockfile, and synchronizes its project library.

## Interactive setup

Run the initializer in a terminal:

```bash
rpx init
```

The interactive flow asks for the target directory, package metadata, license, and development packages. It can add `testthat`, `roxygen2`, and `devtools` to `Suggests`.

If the target is not already inside a Git worktree, rpx also offers to initialize a Git repository. This creates `.git` and an R-focused `.gitignore`; it does not create an initial commit or select a branch.

The target directory must either not exist or be completely empty. rpx does not overwrite or upgrade an existing project. Hidden files, including an existing `.git` directory, count as content.

## Generated project

Initialization creates the package files needed for installation, including:

- `DESCRIPTION`
- `NAMESPACE`
- `.Rbuildignore`
- License files
- `rpx.lock`

It then installs the package and its selected development dependencies into the project library. Commit the generated source files, `DESCRIPTION`, and `rpx.lock`; do not commit the project library.

## Non-interactive setup

Provide a path and metadata flags for deterministic automation:

```bash
rpx init ./mypkg \
  --name mypkg \
  --title "My Package" \
  --description "Utilities for my application." \
  --author-name "Example Author" \
  --author-email "author@example.com" \
  --license mit
```

Supported license values are `mit`, `apache-2.0`, `gpl-2`, `gpl-3`, `agpl-3`, `lgpl-2.1`, `lgpl-3`, `cc0`, `cc-by-4.0`, and `proprietary`.

When no terminal is available, omitted values use defaults, no development packages are selected, and Git is not initialized. Supplying the target path and all metadata flags is recommended for CI or scripts.

## Add a dependency

Move into the generated project and add a package:

```bash
cd mypkg
rpx add digest
rpx run R
```

`rpx add` updates `DESCRIPTION`, resolves and writes `rpx.lock`, and synchronizes the project library. Continue with [Manage dependencies](./05.manage-dependencies.md) for dependency fields and version constraints.
