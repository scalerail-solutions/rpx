---
title: Use an existing project
description: Lock and synchronize an existing R package project.
seo:
  title: Use rpx with an existing R package project
  description: Create a lockfile, install a reproducible project library, and check project state with rpx.
sitemap:
  priority: 0.8
  changefreq: monthly
---

rpx uses an R package `DESCRIPTION` as the project manifest. An existing project needs valid `Package` and `Version` fields. Default synchronization also installs the project, which requires the files needed by `R CMD INSTALL`; dependency-only synchronization does not install it.

## Create the lockfile

Run `rpx lock` from the project or one of its subdirectories:

```bash
rpx lock
```

The command resolves dependencies from `DESCRIPTION` and writes `rpx.lock`. It records the current R version, repository configuration, root requirements, selected packages, and package sources. It does not install packages.

## Synchronize the library

Install the locked package set and the project itself:

```bash
rpx sync
```

Synchronization is strict. It installs missing packages, replaces version mismatches, and removes unexpected packages from the project library. Local projects and Git packages are reinstalled because their contents can change without a version change.

Synchronize only the dependencies when the project itself should not be installed:

```bash
rpx sync --no-install-project
```

This is still exact synchronization. If the project package is already installed, rpx removes it while retaining the locked dependencies. The resulting package set must contain every non-base hard dependency required by the packages being installed.

Before changing the library, rpx validates the complete hard-dependency graph from `Depends`, `Imports`, and `LinkingTo`. Synchronization stops if package metadata is invalid, a required package is missing from the synchronized set, or the graph contains a dependency cycle.

If `DESCRIPTION` or the repository configuration has changed since the lockfile was written, update the lockfile first:

```bash
rpx lock
rpx sync
```

## Check project state

Use `status` before committing or in CI:

```bash
rpx status
```

It checks that `DESCRIPTION`, repository configuration, the current R version, `rpx.lock`, and the locked dependencies in the installed library agree. The project package is optional and its installed version is not checked, so both default and dependency-only environments can be in sync. A project in the expected state prints `Project is in sync`; drift produces a nonzero exit code and a diagnostic.

## System dependencies

rpx does not install operating-system dependencies. Provision required compilers, development headers, and system libraries before running `rpx sync`. Missing system dependencies typically surface while an R package is being built from source.

## CI workflow

A typical CI job restores the exact project state and runs a script in that environment:

```bash
rpx sync
rpx status
rpx run Rscript scripts/check.R
```

## What to commit

Commit both `DESCRIPTION` and `rpx.lock`. Do not commit the isolated library or shared package cache; `rpx sync` recreates the library from the lockfile.

If local state needs to be discarded, run:

```bash
rpx clean
rpx sync
```

`rpx clean` removes the current project library and shared rpx caches, so cached artifacts used by other projects will also need to be downloaded or rebuilt again.

Synchronization is not transactional. rpx removes obsolete or mismatched packages before all new installations finish, so a build or installation failure can leave the library partially synchronized. Fix the reported problem and rerun `rpx sync`; completed downloads, builds, and cache entries can be reused.
